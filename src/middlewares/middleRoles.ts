import { NextFunction, Request, Response } from "express";

class middlewareRoles {

    public checkRoles(req: Request, res: Response, next: NextFunction) {

        // check the role, if necessary
    }
}